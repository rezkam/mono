services:
  postgres:
    image: postgres:18-alpine
    container_name: mono-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-mono}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      POSTGRES_DB: ${POSTGRES_DB:-mono_db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-mono} -d ${POSTGRES_DB:-mono_db}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    # Don't expose port externally in production - only internal network
    networks:
      - mono-network

  migrate:
    image: ghcr.io/rezkam/goose-migrate:v3.26.0
    container_name: mono-migrate
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./internal/infrastructure/persistence/postgres/migrations:/migrations:ro
    command: ["-dir", "/migrations", "postgres", "postgres://${POSTGRES_USER:-mono}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-mono_db}?sslmode=disable", "up"]
    networks:
      - mono-network

  server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mono-server
    environment:
      MONO_STORAGE_DSN: postgres://${POSTGRES_USER:-mono}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-mono_db}?sslmode=disable
      MONO_HTTP_HOST: "0.0.0.0"
      MONO_HTTP_PORT: "8080"
      # Optional tuning
      MONO_DB_MAX_OPEN_CONNS: ${MONO_DB_MAX_OPEN_CONNS:-25}
      MONO_DB_MAX_IDLE_CONNS: ${MONO_DB_MAX_IDLE_CONNS:-10}
      # TLS/HTTPS configuration
      MONO_TLS_ENABLED: ${MONO_TLS_ENABLED:-false}
      MONO_TLS_CERT_FILE: ${MONO_TLS_CERT_FILE:-/certs/cert.pem}
      MONO_TLS_KEY_FILE: ${MONO_TLS_KEY_FILE:-/certs/key.pem}
    volumes:
      - ${MONO_TLS_CERT_FILE_HOST:-./cert.pem}:/certs/cert.pem:ro
      - ${MONO_TLS_KEY_FILE_HOST:-./key.pem}:/certs/key.pem:ro
    ports:
      - "${SERVER_PORT:-80}:8080"
    healthcheck:
      test: ["CMD-SHELL", "if [ \"$MONO_TLS_ENABLED\" = \"true\" ]; then curl -f -k https://localhost:8080/health; else curl -f http://localhost:8080/health; fi"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    restart: unless-stopped
    networks:
      - mono-network

  worker-1:
    build:
      context: .
      dockerfile: Dockerfile.worker
    container_name: mono-worker-1
    environment:
      MONO_STORAGE_DSN: postgres://${POSTGRES_USER:-mono}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-mono_db}?sslmode=disable
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    restart: unless-stopped
    networks:
      - mono-network

  worker-2:
    build:
      context: .
      dockerfile: Dockerfile.worker
    container_name: mono-worker-2
    environment:
      MONO_STORAGE_DSN: postgres://${POSTGRES_USER:-mono}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-mono_db}?sslmode=disable
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    restart: unless-stopped
    networks:
      - mono-network

  apikey:
    build:
      context: .
      dockerfile: Dockerfile.apikey
    container_name: mono-apikey
    environment:
      MONO_STORAGE_DSN: postgres://${POSTGRES_USER:-mono}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-mono_db}?sslmode=disable
      MONO_API_KEY_TYPE: ${MONO_API_KEY_TYPE:-sk}
      MONO_API_SERVICE_NAME: ${MONO_API_SERVICE_NAME:-mono}
      MONO_API_VERSION: ${MONO_API_VERSION:-v1}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - mono-network
    profiles:
      - tools

volumes:
  postgres_data:

networks:
  mono-network:
    driver: bridge
